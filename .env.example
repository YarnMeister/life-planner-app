# Database Configuration
# For local development, use your Neon TEST database connection string
DATABASE_URL=postgresql://user:password@host/dbname?sslmode=require

# Production database (used by Vercel only - leave empty locally)
PROD_DATABASE_URL=

# Optional: Skip RLS tests during first deployment
# Uncomment this line if you need to bypass RLS checks temporarily
# Remove after first successful deployment
# SKIP_RLS_TESTS=1

# Optional: Additional environment variables for your app
# Add your custom environment variables below
